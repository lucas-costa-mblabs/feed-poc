{
  "name": "template-builder",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build:react": "cd client-react && npm run build",
    "build:flutter": "cd client_flutter && flutter build",
    "build:all": "concurrently \"npm run build:react\" \"npm run build:flutter\""
  }
}
